<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Arcistan — Protect your Rupee. Move money without borders.</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;600;700&family=Inter:wght@400;500;600;700&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0D0A0E;
    --bg-2: #16121A;
    --paper: rgba(255,247,235,0.045);
    --paper2: rgba(255,247,235,0.075);
    --ink: #F4EEE4;
    --green: #C9A227;
    --green-soft: #9C7B2E;
    --gold: #E0A73E;
    --teal: #2FB894;
    --violet: #B9773F;
    --line: rgba(255,247,235,0.09);
    --muted: #978F80;
    --red: #E0665A;
  }
  * { box-sizing: border-box; margin: 0; padding: 0; }
  @media (prefers-reduced-motion: reduce) {
    *, *::before, *::after { animation-duration: 0.001ms !important; animation-iteration-count: 1 !important; }
  }
  html { scroll-behavior: smooth; }
  body {
    background: var(--bg); color: var(--ink); font-family: 'Inter', sans-serif;
    line-height: 1.5; -webkit-font-smoothing: antialiased; position: relative; overflow-x: hidden;
  }
  body::before {
    content: ""; position: fixed; inset: -20%; z-index: 0; pointer-events: none;
    background:
      radial-gradient(38% 32% at 18% 8%, rgba(201,162,39,0.16), transparent 65%),
      radial-gradient(42% 38% at 88% 4%, rgba(185,119,63,0.14), transparent 60%),
      radial-gradient(46% 40% at 78% 82%, rgba(47,184,148,0.10), transparent 65%),
      radial-gradient(30% 30% at 4% 78%, rgba(201,162,39,0.08), transparent 60%);
    filter: blur(10px); animation: meshDrift 26s ease-in-out infinite alternate;
  }
  @keyframes meshDrift { 0% { transform: translate3d(0,0,0) scale(1);} 50% { transform: translate3d(-2%,1.5%,0) scale(1.05);} 100% { transform: translate3d(2%,-1%,0) scale(1.02);} }
  a { color: inherit; text-decoration: none; }
  .wrap { max-width: 1120px; margin: 0 auto; padding: 0 24px; position: relative; z-index: 1; }
  section { padding: 88px 0; }
  h1,h2,h3 { font-family: 'Space Grotesk', sans-serif; letter-spacing: -0.01em; }

  /* Nav */
  nav.top {
    position: sticky; top: 0; z-index: 100; backdrop-filter: blur(14px);
    background: linear-gradient(var(--bg) 75%, transparent);
    border-bottom: 1px solid var(--line);
  }
  nav.top .wrap { display: flex; align-items: center; justify-content: space-between; padding: 16px 24px; }
  .brand { display: flex; align-items: center; gap: 10px; }
  .brand-mark {
    width: 34px; height: 34px; border-radius: 10px;
    background: conic-gradient(from 180deg, var(--green), var(--teal), var(--violet), var(--green));
    display: flex; align-items: center; justify-content: center;
    font-weight: 700; font-size: 14px; color: #241A02; box-shadow: 0 6px 18px rgba(201,162,39,0.28);
  }
  .brand-name { font-family: 'Space Grotesk', sans-serif; font-weight: 700; font-size: 19px; }
  .nav-links { display: flex; gap: 28px; font-size: 14px; color: var(--muted); }
  .nav-links a:hover { color: var(--ink); }
  .nav-cta {
    background: linear-gradient(135deg, var(--green), var(--teal)); color: #241A02; font-weight: 600;
    padding: 9px 18px; border-radius: 20px; font-size: 13px; box-shadow: 0 8px 20px -6px rgba(201,162,39,0.4);
  }
  @media (max-width: 720px) { .nav-links { display: none; } }

  /* Hero */
  .hero { padding: 90px 0 70px; text-align: center; }
  .eyebrow {
    display: inline-flex; align-items: center; gap: 8px; font-size: 12px; letter-spacing: 0.1em;
    text-transform: uppercase; color: var(--teal); font-weight: 600;
    border: 1px solid var(--line); background: var(--paper); padding: 7px 14px; border-radius: 20px; margin-bottom: 28px;
  }
  .eyebrow .rt-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--teal); box-shadow: 0 0 8px var(--teal); animation: pulse 1.6s infinite; }
  @keyframes pulse { 0%,100% { opacity:1;} 50% { opacity:0.3;} }
  .hero h1 {
    font-size: 52px; font-weight: 700; line-height: 1.08; max-width: 780px; margin: 0 auto 20px;
  }
  .hero h1 .grad { background: linear-gradient(120deg, var(--green), var(--teal)); -webkit-background-clip: text; background-clip: text; color: transparent; }
  .hero p.sub { font-size: 17px; color: var(--muted); max-width: 560px; margin: 0 auto 34px; }
  .hero-ctas { display: flex; gap: 14px; justify-content: center; flex-wrap: wrap; margin-bottom: 60px; }
  .btn-primary {
    background: linear-gradient(135deg, var(--green), var(--teal)); color: #241A02; font-weight: 600;
    padding: 15px 26px; border-radius: 14px; font-size: 15px; box-shadow: 0 12px 28px -8px rgba(201,162,39,0.45);
    transition: transform 0.2s ease, box-shadow 0.2s ease; display: inline-block;
  }
  .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 16px 34px -8px rgba(201,162,39,0.55); }
  .btn-ghost {
    border: 1px solid var(--line); background: var(--paper); color: var(--ink); font-weight: 600;
    padding: 15px 26px; border-radius: 14px; font-size: 15px; transition: all 0.2s ease; display: inline-block;
  }
  .btn-ghost:hover { border-color: var(--teal); color: var(--teal); }

  /* Signature visual: two coins converging/morphing PKR -> USDC */
  .convert-visual { display: flex; align-items: center; justify-content: center; gap: 0; margin: 0 auto 8px; perspective: 900px; }
  .coin {
    width: 132px; height: 132px; border-radius: 50%; display: flex; flex-direction: column;
    align-items: center; justify-content: center; position: relative; z-index: 2;
    box-shadow: 0 24px 50px -12px rgba(0,0,0,0.5);
    animation: coinFloat 5s ease-in-out infinite;
  }
  .coin.pkr { background: radial-gradient(circle at 32% 28%, #3a4560, #1c2438 70%); border: 1px solid rgba(255,255,255,0.12); animation-delay: 0s; }
  .coin.usdc { background: radial-gradient(circle at 32% 28%, #F0CE7A, var(--green-soft) 55%, #4A3308 100%); animation-delay: 0.4s; }
  .coin .c-sym { font-family: 'IBM Plex Mono', monospace; font-size: 26px; font-weight: 500; color: #FBF6EC; }
  .coin .c-lbl { font-size: 10px; letter-spacing: 0.1em; text-transform: uppercase; color: rgba(251,246,236,0.7); margin-top: 4px; }
  @keyframes coinFloat { 0%,100% { transform: translateY(0) rotateY(0deg); } 50% { transform: translateY(-10px) rotateY(8deg); } }
  .convert-arrow {
    width: 90px; height: 2px; position: relative; margin: 0 -6px; z-index: 1;
    background: linear-gradient(90deg, rgba(255,255,255,0.15), var(--green), var(--teal));
  }
  .convert-arrow::after {
    content: "→"; position: absolute; right: -14px; top: 50%; transform: translateY(-50%);
    color: var(--teal); font-size: 20px;
  }
  @media (max-width: 560px) { .coin { width: 96px; height: 96px; } .convert-arrow { width: 50px; } }

  /* Stats bar */
  .stats-bar {
    display: grid; grid-template-columns: repeat(4, 1fr); gap: 1px;
    background: var(--line); border: 1px solid var(--line); border-radius: 18px; overflow: hidden;
    margin-top: 10px;
  }
  .stat-cell { background: var(--paper); padding: 22px 16px; text-align: center; }
  .stat-cell .num { font-family: 'IBM Plex Mono', monospace; font-size: 24px; font-weight: 500; color: var(--green); }
  .stat-cell .lbl { font-size: 11px; color: var(--muted); margin-top: 4px; text-transform: uppercase; letter-spacing: 0.06em; }
  @media (max-width: 720px) { .stats-bar { grid-template-columns: repeat(2, 1fr); } }

  /* Section heading */
  .section-head { text-align: center; max-width: 620px; margin: 0 auto 48px; }
  .section-head .eyebrow-sm { font-size: 12px; letter-spacing: 0.12em; text-transform: uppercase; color: var(--teal); font-weight: 600; margin-bottom: 12px; }
  .section-head h2 { font-size: 32px; font-weight: 700; margin-bottom: 12px; }
  .section-head p { color: var(--muted); font-size: 15px; }

  /* Two-problems section */
  .problems { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
  .problem-card {
    background: var(--paper); border: 1px solid var(--line); border-radius: 20px; padding: 30px;
    backdrop-filter: blur(16px); transition: transform 0.25s ease, border-color 0.25s ease;
  }
  .problem-card:hover { transform: translateY(-3px); border-color: rgba(255,255,255,0.18); }
  .problem-num { font-family: 'IBM Plex Mono', monospace; font-size: 13px; color: var(--teal); margin-bottom: 14px; }
  .problem-card h3 { font-size: 20px; margin-bottom: 10px; }
  .problem-card p { color: var(--muted); font-size: 14px; margin-bottom: 16px; }
  .problem-fix { display: flex; gap: 10px; align-items: flex-start; font-size: 13px; color: var(--ink); border-top: 1px dashed var(--line); padding-top: 14px; }
  .problem-fix b { color: var(--green); }
  @media (max-width: 720px) { .problems { grid-template-columns: 1fr; } }

  /* Feature grid */
  .feature-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 18px; }
  .feature-card {
    background: var(--paper); border: 1px solid var(--line); border-radius: 18px; padding: 26px;
    backdrop-filter: blur(16px); transition: transform 0.25s ease, box-shadow 0.25s ease, border-color 0.25s ease;
  }
  .feature-card:hover { transform: translateY(-3px); box-shadow: 0 18px 38px -12px rgba(0,0,0,0.4); border-color: rgba(255,255,255,0.16); }
  .f-icon {
    width: 42px; height: 42px; border-radius: 12px; display: flex; align-items: center; justify-content: center;
    font-size: 19px; margin-bottom: 16px; background: linear-gradient(135deg, var(--green), var(--teal)); color: #241A02;
    box-shadow: 0 8px 18px -6px rgba(201,162,39,0.35);
  }
  .feature-card h3 { font-size: 16px; margin-bottom: 8px; font-weight: 600; }
  .feature-card p { font-size: 13px; color: var(--muted); }
  @media (max-width: 900px) { .feature-grid { grid-template-columns: 1fr 1fr; } }
  @media (max-width: 560px) { .feature-grid { grid-template-columns: 1fr; } }

  /* Flagship */
  .flagship {
    display: grid; grid-template-columns: 1fr 1fr; gap: 40px; align-items: center;
    background: linear-gradient(160deg, rgba(201,162,39,0.08), rgba(185,119,63,0.05));
    border: 1px solid rgba(201,162,39,0.2); border-radius: 24px; padding: 48px;
  }
  .flagship-tag {
    display: inline-flex; align-items: center; gap: 6px; font-size: 11px; letter-spacing: 0.08em; text-transform: uppercase;
    color: var(--gold); border: 1px solid rgba(255,194,75,0.4); border-radius: 20px; padding: 4px 12px; margin-bottom: 18px;
  }
  .flagship h2 { font-size: 28px; margin-bottom: 14px; }
  .flagship p.lead { color: var(--muted); font-size: 15px; margin-bottom: 22px; }
  .flagship ul { list-style: none; display: flex; flex-direction: column; gap: 12px; }
  .flagship li { display: flex; gap: 10px; font-size: 14px; align-items: flex-start; }
  .flagship li b { color: var(--green); }
  .flagship-visual {
    position: relative; border-radius: 18px; padding: 34px; background: var(--bg-2); border: 1px solid var(--line);
    text-align: center;
  }
  .fv-ring {
    width: 160px; height: 160px; border-radius: 50%; margin: 0 auto 18px; position: relative;
    background: conic-gradient(var(--green) 0deg, var(--green) 260deg, rgba(255,255,255,0.08) 260deg);
    display: flex; align-items: center; justify-content: center;
    animation: ringGrow 3s ease-in-out infinite alternate;
  }
  @keyframes ringGrow { from { filter: brightness(0.95);} to { filter: brightness(1.1);} }
  .fv-ring::before { content:""; position:absolute; inset:10px; border-radius:50%; background: var(--bg-2); }
  .fv-ring span { position: relative; font-family: 'IBM Plex Mono', monospace; font-size: 26px; color: var(--green); font-weight: 500; }
  .flagship-visual .fv-note { font-size: 12px; color: var(--muted); }
  @media (max-width: 820px) { .flagship { grid-template-columns: 1fr; } }

  /* Network trust */
  .trust-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 18px; }
  .trust-cell { text-align: center; padding: 24px 12px; background: var(--paper); border: 1px solid var(--line); border-radius: 16px; }
  .trust-cell .num { font-family: 'IBM Plex Mono', monospace; font-size: 20px; color: var(--teal); font-weight: 500; }
  .trust-cell .lbl { font-size: 12px; color: var(--muted); margin-top: 6px; }
  @media (max-width: 720px) { .trust-grid { grid-template-columns: repeat(2, 1fr); } }

  /* FAQ */
  .faq-list { max-width: 720px; margin: 0 auto; display: flex; flex-direction: column; gap: 12px; }
  .faq-item { background: var(--paper); border: 1px solid var(--line); border-radius: 14px; overflow: hidden; }
  .faq-q {
    width: 100%; text-align: left; background: none; border: none; color: var(--ink); font-family: 'Space Grotesk', sans-serif;
    font-size: 15px; font-weight: 600; padding: 18px 20px; cursor: pointer; display: flex; justify-content: space-between; align-items: center;
  }
  .faq-q .chev { transition: transform 0.25s ease; color: var(--teal); }
  .faq-item.open .faq-q .chev { transform: rotate(45deg); }
  .faq-a { max-height: 0; overflow: hidden; transition: max-height 0.3s ease; }
  .faq-item.open .faq-a { max-height: 220px; }
  .faq-a p { padding: 0 20px 18px; font-size: 13.5px; color: var(--muted); }

  footer { border-top: 1px solid var(--line); padding: 56px 0 40px; }
  .foot-grid { display: grid; grid-template-columns: 1.6fr 1fr 1fr; gap: 40px; margin-bottom: 40px; }
  .foot-col h4 { font-size: 13px; color: var(--muted); text-transform: uppercase; letter-spacing: 0.08em; margin-bottom: 14px; }
  .foot-col a, .foot-col p { display: block; font-size: 13.5px; color: var(--muted); margin-bottom: 10px; }
  .foot-col a:hover { color: var(--ink); }
  .foot-bottom { display: flex; justify-content: space-between; align-items: center; font-size: 12px; color: var(--muted); flex-wrap: wrap; gap: 10px; }
  .foot-badge { display: inline-flex; align-items: center; gap: 6px; }
  .net-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--green); box-shadow: 0 0 6px var(--green); }
  @media (max-width: 720px) { .foot-grid { grid-template-columns: 1fr; } }
</style>
</head>
<body>

<nav class="top">
  <div class="wrap">
    <div class="brand">
      <div class="brand-mark">A</div>
      <div class="brand-name">Arcistan</div>
    </div>
    <div class="nav-links">
      <a href="#problems">Why Arcistan</a>
      <a href="#features">Features</a>
      <a href="#network">Network</a>
      <a href="#faq">FAQ</a>
    </div>
    <a href="/app" class="nav-cta">Launch App</a>
  </div>
</nav>

<div class="wrap">

  <!-- HERO -->
  <section class="hero">
    <div class="eyebrow"><span class="rt-dot"></span> Live on Arc Testnet</div>
    <h1>Protect your Rupee.<br>Move money <span class="grad">without borders.</span></h1>
    <p class="sub">Arcistan converts PKR to USDC instantly and gives freelancers and overseas Pakistanis a direct payment gateway — no bank account, no paperwork, no delays.</p>
    <div class="hero-ctas">
      <a href="/app" class="btn-primary">Launch App</a>
      <a href="#problems" class="btn-ghost">See how it works</a>
    </div>

    <div class="convert-visual">
      <div class="coin pkr"><div class="c-sym">₨</div><div class="c-lbl">PKR</div></div>
      <div class="convert-arrow"></div>
      <div class="coin usdc"><div class="c-sym">$</div><div class="c-lbl">USDC</div></div>
    </div>

    <div class="stats-bar">
      <div class="stat-cell"><div class="num">15-25%</div><div class="lbl">PKR lost to inflation / yr</div></div>
      <div class="stat-cell"><div class="num">$0.01 flat</div><div class="lbl">Remittance fee</div></div>
      <div class="stat-cell"><div class="num">0.1%</div><div class="lbl">Swap fee</div></div>
      <div class="stat-cell"><div class="num">&lt;1s</div><div class="lbl">Arc transaction finality</div></div>
    </div>
  </section>

  <!-- TWO PROBLEMS -->
  <section id="problems">
    <div class="section-head">
      <div class="eyebrow-sm">The Problem</div>
      <h2>Two things broken about money in Pakistan</h2>
      <p>Arcistan exists to fix both, on the same rail.</p>
    </div>
    <div class="problems">
      <div class="problem-card">
        <div class="problem-num">01</div>
        <h3>Your savings quietly lose value</h3>
        <p>The Rupee loses 15-25% of its value every year. Money sitting in a bank account or under a mattress is losing purchasing power even when the number on the screen stays the same.</p>
        <div class="problem-fix"><b>Fix:</b> Convert PKR to USDC in seconds — your savings hold stable dollar value instead of eroding silently.</div>
      </div>
      <div class="problem-card">
        <div class="problem-num">02</div>
        <h3>Freelancers have no direct payment gateway</h3>
        <p>Pakistan has no simple cross-border payment platform built for its freelancers and remote workers. Getting paid by an international client, or sending money home from abroad, means bank wire delays, high cuts, and paperwork — or relying on informal channels.</p>
        <div class="problem-fix"><b>Fix:</b> Get paid straight into a USDC wallet — a flat $0.01 fee, no bank in the middle, arrives in under a second, from anywhere in the world.</div>
      </div>
    </div>
  </section>

  <!-- FEATURES -->
  <section id="features">
    <div class="section-head">
      <div class="eyebrow-sm">What's Inside</div>
      <h2>Everything you need to save and move money</h2>
      <p>One wallet, one balance, built for everyday use — not just holding.</p>
    </div>
    <div class="feature-grid">
      <div class="feature-card">
        <div class="f-icon">⇄</div>
        <h3>Instant Convert</h3>
        <p>Swap PKR to USDC and back, anytime, at a live rate — just a 0.1% fee, no waiting, no lock-in.</p>
      </div>
      <div class="feature-card">
        <div class="f-icon">✈</div>
        <h3>Get Paid From Abroad</h3>
        <p>Freelancers and overseas Pakistanis receive USDC straight into their wallet from clients or family — a flat $0.01 fee, far below bank wire costs.</p>
      </div>
      <div class="feature-card">
        <div class="f-icon">↻</div>
        <h3>Auto-Save</h3>
        <p>Recurring PKR→USDC conversion, so protecting your savings doesn't take manual effort every month.</p>
      </div>
      <div class="feature-card">
        <div class="f-icon">◎</div>
        <h3>Goal Pockets</h3>
        <p>Protect savings for a specific purpose — rent, a trip, an emergency fund — without it all sitting in one lump.</p>
      </div>
      <div class="feature-card">
        <div class="f-icon">◫</div>
        <h3>Group Savings</h3>
        <p>Save toward a shared goal with family or friends, with every contribution tracked transparently.</p>
      </div>
      <div class="feature-card">
        <div class="f-icon">▦</div>
        <h3>QR Pay, Top-Up & Bills</h3>
        <p>Spend directly from your USDC balance — pay utility bills, top up mobile credit, or pay by QR — without converting back to PKR first.</p>
      </div>
      <div class="feature-card">
        <div class="f-icon">💎</div>
        <h3>Milestones &amp; ArcPoints</h3>
        <p>Every swap, bill, and deposit earns ArcPoints — set savings milestones, track progress, and climb from Starter to Arcistan Elite.</p>
      </div>
    </div>
  </section>

  <!-- FLAGSHIP -->
  <section>
    <div class="flagship">
      <div>
        <div class="flagship-tag">Flagship Feature</div>
        <h2>Protection that happens on its own</h2>
        <p class="lead">Auto-Save moves a set amount from PKR to USDC on a schedule you choose — so your money starts protecting itself the moment you turn it on.</p>
        <ul>
          <li><b>Set once</b> — pick an amount and a frequency, weekly or monthly.</li>
          <li><b>Runs automatically</b> — no need to remember or convert manually.</li>
          <li><b>Withdraw anytime</b> — nothing is locked in, ever.</li>
        </ul>
      </div>
      <div class="flagship-visual">
        <div class="fv-ring"><span>72%</span></div>
        <div class="fv-note">Sample: monthly savings goal protected on autopilot</div>
      </div>
    </div>
  </section>

  <!-- NETWORK / TRUST -->
  <section id="network">
    <div class="section-head">
      <div class="eyebrow-sm">Built On</div>
      <h2>Arc Network, by Circle</h2>
      <p>Arcistan runs on Arc — a blockchain purpose-built for stablecoin payments.</p>
    </div>
    <div class="trust-grid">
      <div class="trust-cell"><div class="num">&lt;1s</div><div class="lbl">Transaction finality</div></div>
      <div class="trust-cell"><div class="num">USDC</div><div class="lbl">Native gas asset</div></div>
      <div class="trust-cell"><div class="num">~$0.01</div><div class="lbl">Per-transaction fee</div></div>
      <div class="trust-cell"><div class="num">Testnet</div><div class="lbl">Current network stage</div></div>
    </div>
  </section>

  <!-- FAQ -->
  <section id="faq">
    <div class="section-head">
      <div class="eyebrow-sm">Questions</div>
      <h2>Frequently asked</h2>
    </div>
    <div class="faq-list" id="faqList">
      <div class="faq-item">
        <button class="faq-q">Can I use Arcistan to get paid by an international client? <span class="chev">+</span></button>
        <div class="faq-a"><p>Yes — a client anywhere in the world can send USDC directly to your Arcistan wallet. No Payoneer/Wise-style approval process, no waiting days for a bank transfer to clear.</p></div>
      </div>
      <div class="faq-item">
        <button class="faq-q">How is Arcistan different from Payoneer or a bank wire? <span class="chev">+</span></button>
        <div class="faq-a"><p>Payoneer and bank wires route through multiple intermediaries, each taking a cut and adding delay. Arcistan settles directly on Arc Network — a flat ~$0.01 fee and under a second, with no intermediary bank involved.</p></div>
      </div>
      <div class="faq-item">
        <button class="faq-q">Is my money actually converted, or is this just a display number? <span class="chev">+</span></button>
        <div class="faq-a"><p>Every convert, deposit, and withdraw is a real transaction on Arc testnet — your balance reflects an actual on-chain USDC holding, not just a number on screen.</p></div>
      </div>
      <div class="faq-item">
        <button class="faq-q">Why USDC instead of holding cash or gold? <span class="chev">+</span></button>
        <div class="faq-a"><p>USDC tracks the US Dollar directly, moves instantly, and can be spent or sent without needing a bank. Cash under a mattress still loses value to inflation; gold isn't easy to move or spend in small amounts.</p></div>
      </div>
      <div class="faq-item">
        <button class="faq-q">Is this legal in Pakistan? <span class="chev">+</span></button>
        <div class="faq-a"><p>Arcistan is currently a testnet product for building and demonstration purposes. Regulation around crypto assets in Pakistan is still developing — this isn't legal advice, and real-money use would depend on that regulatory picture.</p></div>
      </div>
      <div class="faq-item">
        <button class="faq-q">What happens if I want my Rupees back? <span class="chev">+</span></button>
        <div class="faq-a"><p>You can swap USDC back to PKR anytime — nothing is locked in, and there's no minimum holding period.</p></div>
      </div>
      <div class="faq-item">
        <button class="faq-q">What fees does Arcistan charge? <span class="chev">+</span></button>
        <div class="faq-a"><p>Just <b>0.1%</b> to swap PKR ↔ USDC, and a flat <b>$0.01</b> to send money abroad — plus a tiny Arc network gas fee (usually a fraction of a cent) that goes to the network itself, not to us. No hidden charges, no monthly fees, no minimum balance.</p></div>
      </div>
      <div class="faq-item">
        <button class="faq-q">Do I need a bank account to use Arcistan? <span class="chev">+</span></button>
        <div class="faq-a"><p>No. You only need a wallet. That's the core idea — access without needing to go through a bank first.</p></div>
      </div>
    </div>
  </section>

</div>

<footer>
  <div class="wrap">
    <div class="foot-grid">
      <div class="foot-col">
        <div class="brand" style="margin-bottom:14px;">
          <div class="brand-mark">A</div>
          <div class="brand-name">Arcistan</div>
        </div>
        <p style="max-width:280px;">Protect your Rupee and move money without borders — built on Arc Network.</p>
      </div>
      <div class="foot-col">
        <h4>Product</h4>
        <a href="#features">Features</a>
        <a href="#faq">FAQ</a>
        <a href="/app">Launch App</a>
      </div>
      <div class="foot-col">
        <h4>Resources</h4>
        <a href="#faq">FAQ</a>
        <a href="#">Blog (coming soon)</a>
        <a href="#">Docs (coming soon)</a>
      </div>
    </div>
    <div class="foot-bottom">
      <span>© 2026 Arcistan</span>
      <span class="foot-badge"><span class="net-dot"></span> Arc Testnet — Live</span>
    </div>
  </div>
</footer>

<script>
  document.querySelectorAll('.faq-q').forEach(function(btn){
    btn.addEventListener('click', function(){
      var item = btn.closest('.faq-item');
      var wasOpen = item.classList.contains('open');
      document.querySelectorAll('.faq-item.open').forEach(function(i){ i.classList.remove('open'); });
      if (!wasOpen) item.classList.add('open');
    });
  });
</script>

</body>
</html>
